const mysql = require('mysql');

//not yet working because I haven't installed mysql on my pc
/*
const con = mysql.createConnection({
  host: 'localhost',
  user: 'myusername',
  password: 'mypassword',
});

con.connect(function (err) {
  if (err) {
    console.log(err);
    return;
  }
  console.log('Connected!');
});
*/
